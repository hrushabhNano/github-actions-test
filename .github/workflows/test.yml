# real test yml file
name: Test GitHub Action

# deployment_status
on: [push]
jobs: 
    build-app:
      runs-on: ubuntu-latest # runs on a docker container
      steps: 
          - uses: actions/checkout@v2
          - run: tree

